<section class="card">
  <a href="/shoe/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>By: {{brand}}</p>
    </div>
  </a>
</section>
